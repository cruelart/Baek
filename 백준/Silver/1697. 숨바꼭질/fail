//#include <iostream>
//#include <vector>
//#include <queue>
//
//using namespace std;
//
//int BFS(int _N, int _K, vector<bool>& _isVisit)
//{
//	queue<int> bfs_queue;
//	bfs_queue.push(_N);
//
//	int previous_location = _N;
//	int count = 0; //반복 횟수
//
//	while (!bfs_queue.empty())
//	{
//		int present_location = bfs_queue.front(); // 현재 좌표
//		bfs_queue.pop();
//		_isVisit[present_location] = true;
//
//		if (present_location == _K)
//		{
//			return count; // 동생을 발견했다
//		}
//
//		if (present_location - 1 >= 0 && !_isVisit[present_location - 1])
//		{
//			bfs_queue.push(present_location - 1);
//		}
//		if ( present_location + 1 <= 100000 &&!_isVisit[present_location + 1])
//		{
//			bfs_queue.push(present_location + 1);
//		}
//		if (2 * present_location <= 100000 && !_isVisit[2 * present_location])
//		{
//			bfs_queue.push(2 * present_location);
//		}
//
//		if (count == 0 && present_location == _N)
//		{
//			count++;
//			continue;
//		}
//		if (present_location == previous_location * 2 || previous_location == 0)
//		{
//			previous_location = present_location;
//			count++;
//		}
//	}
//	return 0;
//}
//
//int main()
//{
//	ios::sync_with_stdio(false);
//	cin.tie(nullptr);
//
//	int N, K; // 수빈이의 위치, 동생의 위치
//	cin >> N >> K;
//
//	vector<bool> isVisit(100001, false);
//
//	cout << BFS(N, K, isVisit);
//
//	return 0;
//}

//코드의 문제점

/*
현재 이코드는 굉장히 불안정한 코드 거의 실패한 코드라고 할 수 있다
why? -> N의 값이 충분히 크다면 대충 5만이 넘어갈경우 2배인 10만을 넘기기때문에 bfs_queue에 push되지않는다.
그렇게 되면 count는 증가하지 않기때문에 count로 초를 세는 이 코드는 제 기능을 하지 못할 수 밖에 없다.
하지만 전반적인 코드는 맞지만 count를 세는 부분에서 문제가 발생함
*/
