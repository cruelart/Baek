#include <iostream>

using namespace std;

int main()
{
	int n;
	cin >> n;

	int count_num = 0;

	while (n > 0)
	{
		if (n % 5 == 0)
		{
			count_num += n / 5;
			break;
		}
		
		n -= 2;
		count_num++;
	}
	if (n < 0)
	{
		cout << -1;
		return 0;
	}

	cout << count_num;

	return 0;
}
